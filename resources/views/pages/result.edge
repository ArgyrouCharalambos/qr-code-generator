<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      Détails
    </title>

    <link rel="preconnect" href="https://fonts.bunny.net" />
    <link
      href="https://fonts.bunny.net/css?family=instrument-sans:400,400i,500,500i,600,600i,700,700i"
      rel="stylesheet"
    />

    <style>
      :root {
        --sand-1: #fdfdfc;
        --sand-2: #f9f9f8;
        --sand-3: #f1f0ef;
        --sand-4: #e9e8e6;
        --sand-5: #e2e1de;
        --sand-6: #dad9d6;
        --sand-7: #cfceca;
        --sand-8: #bcbbb5;
        --sand-9: #8d8d86;
        --sand-10: #82827c;
        --sand-11: #63635e;
        --sand-12: #21201c;
      }
    </style>

    <script src="https://cdn.tailwindcss.com">
      
    </script>

    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: [ "Instrument Sans", "sans-serif" ]
            },
            colors: {
              primary: {
                DEFAULT: "#5A45FF",
                lighter: "#a599ff"
              },
              sand: {
                1: "var(--sand-1)",
                2: "var(--sand-2)",
                3: "var(--sand-3)",
                4: "var(--sand-4)",
                5: "var(--sand-5)",
                6: "var(--sand-6)",
                7: "var(--sand-7)",
                8: "var(--sand-8)",
                9: "var(--sand-9)",
                10: "var(--sand-10)",
                11: "var(--sand-11)",
                12: "var(--sand-12)"
              }
            }
          }
        }
      };
    </script>

    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @stack('dumper')
  </head>
  <body class="bg-sand-2">
    @!component('components/header')
    <main class="container mx-auto px-4 max-w-3xl pb-[100px]">
      <div class="bg-white rounded-2xl shadow-sm border border-sand-3 p-8 md:p-12">
          <h2 class="text-3xl font-bold text-sand-12 mb-8 text-center">
              Votre lien est prêt !
          </h2>

          <div class="flex flex-col items-center mb-12">
              @each(QRCode in Qrlien)
              <div class="p-4 border-2 border-sand-4 rounded-xl shadow-inner">
                  <img src="{{ QRCode }}" alt="QR Code" class="w-48 h-48 object-contain">
              </div>
              @end
          </div>

          <div class="space-y-8">
              @each(AncUrl in lien)
              <div>
                  <p class="text-sm text-sand-10 mb-2">Ancien lien</p>
                  <div class="flex items-center gap-4 bg-sand-2 p-4 rounded-lg">
                      <a href="{{ AncUrl }}" 
                         class="text-sand-12 text-sm break-all hover:text-primary transition underline-offset-4 hover:underline">
                          {{ AncUrl }}
                      </a>
                  </div>
              </div>
              @end

              @each(newUrls in newUrl)
              <div>
                  <p class="text-sm text-sand-10 mb-2">Nouveau lien</p>
                  <div class="bg-sand-2 p-4 rounded-lg">
                      <a href="{{ newUrls }}" 
                         class="text-primary text-sm font-medium break-all hover:underline underline-offset-4">
                          {{ newUrls }}
                      </a>
                  </div>
              </div>
              @end
          </div>
      </div>

  </main>
  @!component('components/footer')
  </body>
</html>
